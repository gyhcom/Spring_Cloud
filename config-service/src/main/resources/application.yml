server:
  port: 8888

spring:
  profiles:
    active: native
  application:
    name: config-service
  cloud:
    config:
      server:
        native:
          search-locations: file:///Users/gyh/Documents/Workspace/cloud/git-local-repo
        #          username: <github-id>
        #          password: <gihub-accessToken>
        bootstrap: true